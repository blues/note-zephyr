# OpenOCD

Configuration files here are provided for running a standalone OpenOCD server. This can be used for flashing and debugging the Swan R5 from within the VSCode devcontainer.

## Usage

```bash
openocd -f swan_r5.cfg
```